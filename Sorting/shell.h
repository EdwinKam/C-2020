#ifndef __SHELL_H__
#define __SHELL_H__
#include "array.h"
#include <stdio.h>
void Shell_Sort(Array *);       // take the unsorted array
void gap(uint32_t *, uint32_t); // take an empty array and its size
#endif
